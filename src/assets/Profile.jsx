import React from 'react';
import Login from './Login';
import './All.css'; // Optional: if you have specific styles for Profile component

const Profile = () => {
  return (
    <div className="profile-container">
      <Login />
    </div>
  );
};

export default Profile;
